version: 2.1
jobs:
  build:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run:
          name: Build and Run Tests
          command: mvn clean test
workflows:
  sample:
    jobs:
      - build